const Meme = require("../../models/memes");

const p1score=0;

function myFunction(x) {
    x.classList.toggle("abc");
    x.classList.add("fa fa-thumbs-up");
    meme.like = meme.like + 1;
    meme.save();
}